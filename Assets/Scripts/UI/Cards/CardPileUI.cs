using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Zoca.UI
{
    /// <summary>
    /// This class gives some methods to manage a pile of cards, more specifically to arrange instances of
    /// CardUI on the table.
    /// </summary>
    public class CardPileUI : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }

}
